import { LinkUsernameDirective } from './link-username.directive';

describe('LinkUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
