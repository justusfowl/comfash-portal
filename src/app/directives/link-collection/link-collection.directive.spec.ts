import { LinkCollectionDirective } from './link-collection.directive';

describe('LinkCollectionDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkCollectionDirective();
    expect(directive).toBeTruthy();
  });
});
